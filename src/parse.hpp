#pragma once

#include <string>
#include <vector>

#include "program.hpp"

using namespace std;

namespace parse {

program::function parse(const vector<lex::token> &tokens) {
  program::function result;
  return result;
}

}
